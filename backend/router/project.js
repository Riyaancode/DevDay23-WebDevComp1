const express = require("express");
const app = express();
const project = require("../controller/project");

// Add project 
// app.post("/add", project.addproject);

// Get All project
// app.get("/get/:userID", project.getAllProjects)

module.exports = app;
